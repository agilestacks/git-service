apiVersion: v1
kind: Secret
metadata:
  name: gits-secrets
  namespace: ${component.git-service.namespace}
  labels:
    provider: agilestacks.com
    project: ${component.git-service.namespace}
data:
  gits-key: ${component.git-service.privatekey.base64Encoded}
  hub_api_secret: ${component.git-service.hubApiSecret.base64Encoded}
  auth_api_secret: ${component.git-service.authApiSecret.base64Encoded}
  git_api_secret: ${component.git-service.gitApiSecret.base64Encoded}